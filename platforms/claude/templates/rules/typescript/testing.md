# TypeScript/JavaScript Testing

## E2E Testing

Use **Playwright** as the E2E testing framework for critical user flows.

## Agent Support

- Use a separately installed E2E-testing workflow when available; this rule set does not provide one.
