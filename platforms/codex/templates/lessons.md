# Global Lessons (Codex)

> Cross-project corrections for Codex only. Append date, context, mistake and a concrete rule.
> Project-specific corrections belong in `<project-root>/lessons.md`.
> Locate the root with `git rev-parse --show-toplevel`, falling back to the workspace root outside Git.

---

<!-- Append global corrections below. Keep Claude lessons in its own home. -->
